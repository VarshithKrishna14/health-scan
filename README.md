<!-- # HealthScan - AI Medical Image Analyzer
A Flask-based web application for analyzing medical scan images using AI.

## Features:
✔ Detects anomalies in medical images  
✔ Uses CNN model trained on preprocessed datasets  
✔ Provides a user-friendly web interface for doctors  

## Installation
```bash
pip install -r requirements.txt
python model_train.py  # Train the model
python app.py  # Run the web app -->
